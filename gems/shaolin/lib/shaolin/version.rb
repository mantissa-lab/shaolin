module Shaolin
  # Version of the umbrella `shaolin` gem. The sub-gems share the same 0.2.0 line
  # and the umbrella pins them to exactly this version, so the whole framework
  # moves in lockstep (like `rails` pinning `activerecord` == the Rails version).
  VERSION = "0.2.0".freeze
end
