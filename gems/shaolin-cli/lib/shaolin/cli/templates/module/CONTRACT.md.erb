# <%= @name %> — module contract

- **Commands handled:** `<%= @ns %>::Commands::<%= @command %>`
- **Events published:** `<%= @topic %>`
- **HTTP routes:** `POST /<%= @name %>`, `GET /<%= @name %>/:id`
- **Read model:** `<%= @read_table %>`

This file is the module's public boundary — read it instead of the internals.
